<ul class="media-list">
  <% tasks.each do |task| %>
    <% user = task.user %>
    <li class="media">
      <div class="media-body">
        <div>
          <p><%= link_to task.id, task %> : <%= task.content %> ＞ <%= task.status %></p>
        </div>
      </div>
    </li>
  <% end %>
  <%#= paginate tasks %>
</ul>